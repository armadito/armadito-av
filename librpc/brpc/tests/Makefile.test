BINS=rpc-server-test rpc-client-test rpc-client-benchmark
CFLAGS=-Wall -Wno-unused -I../include/ -DHAVE_PTHREAD
ifdef debug
CFLAGS+=-g -DDEBUG
endif
LDLIBS=-lm -lpthread 
OBJS=../buffer.o ../hash.o ../brpc.o unix.o

all: $(BINS)

$(BINS): $(OBJS)

clean:
	-rm $(OBJS) $(BINS)
